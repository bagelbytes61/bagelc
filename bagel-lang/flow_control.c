#include "flow_control.h"

#include "builtin_types.h"

#include <stddef.h>

struct c_ast_node *c_ast_evaluate_if(struct c_ast_node *node, struct c_ast_context *context) {
    //struct c_ast_node *conditional_result = c_ast_node_evaluate(c_ast_if_exp(node), context);
    //if (conditional_result->value != 0) {
    //    c_ast_node_evaluate(c_ast_if_true_path(node), context);
    //}
    //else {
    //    c_ast_node_evaluate(c_ast_if_false_path(node), context);
    //}

    return NULL;
}